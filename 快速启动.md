# 🚀 本地快速启动Streamlit演示

## 方法1：一键启动脚本（推荐）

在PowerShell中执行：

```powershell
cd d:\mlflow_learning_project
.\本地启动Streamlit.ps1
```

---

## 方法2：直接命令启动

### 2.1 基本启动（仅本地访问）

```powershell
cd d:\mlflow_learning_project
streamlit run material_generator_app_optimized.py --server.port 8501
```

**访问地址：** `http://localhost:8501`

---

### 2.2 允许局域网访问（老板在同一WiFi下可访问）

```powershell
cd d:\mlflow_learning_project
streamlit run material_generator_app_optimized.py --server.port 8501 --server.address 0.0.0.0
```

**访问地址：**
- 本地：`http://localhost:8501`
- 局域网：`http://你的IP:8501`（需要查看你的IP地址）

**查看你的IP地址：**
```powershell
ipconfig | findstr IPv4
```

---

## 方法3：使用优化后的代码启动

如果文件路径不同，使用实际路径：

```powershell
cd d:\mlflow_learning_project

# 如果文件在app/web目录下
streamlit run app\web\material_generator_app.py --server.port 8501 --server.address 0.0.0.0

# 或者如果文件在根目录
streamlit run material_generator_app_optimized.py --server.port 8501 --server.address 0.0.0.0
```

---

## 📱 给老板演示

### 场景1：老板在旁边看你的电脑
- 直接启动，访问 `http://localhost:8501`
- 浏览器会自动打开

### 场景2：老板用手机/其他电脑访问
1. 启动时使用 `--server.address 0.0.0.0`
2. 查看你的IP地址：`ipconfig | findstr IPv4`
3. 告诉老板访问：`http://你的IP:8501`
4. **确保你和老板在同一WiFi网络下**

---

## ⚡ 最快启动方式

直接复制粘贴这个命令：

```powershell
cd d:\mlflow_learning_project; streamlit run material_generator_app_optimized.py --server.port 8501 --server.address 0.0.0.0
```

---

## 🛑 停止服务

按 `Ctrl + C` 停止Streamlit服务

---

## 💡 提示

- 首次启动可能需要几秒钟加载模型
- 如果端口被占用，可以换其他端口：`--server.port 8502`
- 确保已安装所有依赖：`pip install streamlit plotly pandas numpy pillow torch`

