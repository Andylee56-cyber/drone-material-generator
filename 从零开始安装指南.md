# 🚁 大疆无人机视觉智能Agent系统 - 从零开始安装指南

**海南博悦科技有限公司 | 实习生完整安装教程**

---

## 📋 目录

1. [准备工作](#准备工作)
2. [第一步：安装Python](#第一步安装python)
3. [第二步：安装Git](#第二步安装git)
4. [第三步：创建项目目录](#第三步创建项目目录)
5. [第四步：创建Python虚拟环境](#第四步创建python虚拟环境)
6. [第五步：安装Python依赖包](#第五步安装python依赖包)
7. [第六步：创建项目结构](#第六步创建项目结构)
8. [第七步：初始化数据库](#第七步初始化数据库)
9. [第八步：配置系统](#第八步配置系统)
10. [第九步：启动系统](#第九步启动系统)
11. [第十步：验证系统](#第十步验证系统)
12. [常见问题解决](#常见问题解决)

---

## 📦 准备工作

### 检查你的电脑

在开始之前，请确认：

- ✅ **操作系统**: Windows 10 或 Windows 11
- ✅ **管理员权限**: 能够以管理员身份运行PowerShell
- ✅ **网络连接**: 能够访问互联网下载软件和包
- ✅ **磁盘空间**: 至少预留 3GB 可用空间

### 需要安装的软件清单

1. **Python 3.9 或更高版本** ⭐ 必须
2. **Git** ⭐ 推荐（用于DVC）
3. **Anaconda/Miniconda** ⭐ 推荐（可选，但强烈推荐）

---

## 第一步：安装Python

### 1.1 检查是否已安装Python

打开PowerShell（按 `Win + X`，选择"Windows PowerShell"或"终端"），输入：

```powershell
python --version
```

**预期结果：**
- 如果显示 `Python 3.9.x` 或更高版本 → ✅ 已安装，跳过到步骤1.3
- 如果显示 `Python 2.x` → ❌ 版本太旧，需要安装新版本
- 如果显示 `'python' 不是内部或外部命令` → ❌ 未安装，继续步骤1.2

### 1.2 下载并安装Python

1. **访问Python官网**
   - 打开浏览器，访问：https://www.python.org/downloads/
   - 或者直接访问：https://www.python.org/downloads/windows/

2. **下载Python**
   - 点击"Download Python 3.x.x"（选择最新稳定版，推荐3.9或3.10）
   - 下载完成后，你会得到一个 `.exe` 文件

3. **安装Python**
   - **重要**: 双击下载的 `.exe` 文件
   - ✅ **勾选** "Add Python to PATH"（这一步非常重要！）
   - 点击 "Install Now"
   - 等待安装完成（可能需要几分钟）

4. **验证安装**
   - 关闭并重新打开PowerShell（重要！）
   - 输入：
   ```powershell
   python --version
   ```
   - 应该显示：`Python 3.9.x` 或类似

5. **验证pip（Python包管理器）**
   ```powershell
   pip --version
   ```
   - 应该显示：`pip 22.x.x` 或类似

### 1.3 如果安装失败怎么办？

**问题1：安装时没有勾选"Add Python to PATH"**
- **解决方法**：
  1. 卸载Python
  2. 重新安装，这次务必勾选 "Add Python to PATH"

**问题2：安装后仍然提示"python不是内部命令"**
- **解决方法**：
  1. 找到Python安装路径（通常在 `C:\Users\你的用户名\AppData\Local\Programs\Python\Python39\`）
  2. 将该路径添加到系统环境变量PATH中：
     - 右键"此电脑" → "属性" → "高级系统设置" → "环境变量"
     - 在"系统变量"中找到"Path"，点击"编辑"
     - 点击"新建"，添加Python路径
     - 确定保存，重新打开PowerShell

---

## 第二步：安装Git（推荐）

### 2.1 为什么需要Git？

Git用于代码版本控制和DVC数据版本管理。虽然不是必须的，但强烈推荐安装。

### 2.2 检查是否已安装Git

```powershell
git --version
```

- 如果显示版本号 → ✅ 已安装，跳过到第三步
- 如果显示错误 → ❌ 未安装，继续安装

### 2.3 下载并安装Git

1. **访问Git官网**
   - 打开浏览器，访问：https://git-scm.com/download/win
   - 下载会自动开始

2. **安装Git**
   - 双击下载的安装程序
   - 一直点击"Next"（使用默认设置即可）
   - 安装完成后，关闭并重新打开PowerShell

3. **验证安装**
   ```powershell
   git --version
   ```
   - 应该显示：`git version 2.x.x`

---

## 第三步：创建项目目录

### 3.1 选择项目位置

建议在以下位置创建项目：
- `D:\dji_drone_vision_agent` （推荐）
- 或 `C:\Users\你的用户名\dji_drone_vision_agent`

### 3.2 创建项目目录

打开PowerShell，执行：

```powershell
# 切换到D盘（或你选择的盘符）
cd D:\

# 创建项目目录
mkdir dji_drone_vision_agent

# 进入项目目录
cd dji_drone_vision_agent

# 验证目录创建成功
pwd
```

**预期结果**：显示 `D:\dji_drone_vision_agent` 或你的路径

### 3.3 验证当前目录

```powershell
# 查看当前目录内容（应该是空的）
dir
```

---

## 第四步：创建Python虚拟环境

### 4.1 为什么需要虚拟环境？

虚拟环境可以隔离项目依赖，避免不同项目之间的包冲突。

### 4.2 创建虚拟环境

在项目目录中执行：

```powershell
# 确保在项目目录中
cd D:\dji_drone_vision_agent

# 创建虚拟环境（命名为 venv）
python -m venv venv
```

**这个过程可能需要1-2分钟，请耐心等待。**

### 4.3 激活虚拟环境

```powershell
# 激活虚拟环境
.\venv\Scripts\activate
```

**激活成功后，你的PowerShell提示符前面会出现 `(venv)` 标识：**

```
(venv) PS D:\dji_drone_vision_agent>
```

### 4.4 验证虚拟环境

```powershell
# 检查Python路径（应该指向venv目录）
python --version

# 检查pip路径（应该指向venv目录）
pip --version
```

**如果看到路径包含 `venv`，说明虚拟环境已激活成功！**

### 4.5 升级pip（重要）

```powershell
# 升级pip到最新版本
python -m pip install --upgrade pip
```

**等待升级完成，可能需要1-2分钟。**

---

## 第五步：安装Python依赖包

### 5.1 创建requirements文件

在项目目录中创建 `requirements_agent.txt` 文件。

**方法1：使用PowerShell创建**

```powershell
# 确保在项目目录中，且虚拟环境已激活
cd D:\dji_drone_vision_agent
.\venv\Scripts\activate

# 创建requirements文件
@"
# 大疆无人机视觉智能Agent系统 - 依赖包列表
# 海南博悦科技有限公司

# 深度学习框架
torch>=1.12.0
torchvision>=0.13.0

# 数据处理
numpy>=1.21.0
pandas>=1.3.0
opencv-python>=4.6.0
Pillow>=9.0.0

# 实验跟踪
mlflow>=1.30.0

# 数据版本控制
dvc>=2.10.0

# Web框架
streamlit>=1.12.0
plotly>=5.10.0

# 数据库
sqlalchemy>=1.4.0

# 安全
cryptography>=37.0.0

# 工具
pyyaml>=6.0
python-dotenv>=0.19.0

# 可视化
matplotlib>=3.5.0
seaborn>=0.12.0

# 机器学习工具
scikit-learn>=1.0.0
"@ | Out-File -FilePath requirements_agent.txt -Encoding utf8
```

**方法2：手动创建**

1. 在项目目录中右键 → "新建" → "文本文档"
2. 命名为 `requirements_agent.txt`（注意扩展名）
3. 打开文件，复制粘贴上面的内容
4. 保存文件

### 5.2 验证requirements文件

```powershell
# 查看文件内容（应该显示依赖列表）
type requirements_agent.txt
```

### 5.3 安装依赖包（使用国内镜像，速度更快）

```powershell
# 确保虚拟环境已激活
.\venv\Scripts\activate

# 使用清华镜像源安装（推荐，速度快）
pip install -r requirements_agent.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

**⚠️ 重要提示：**
- 这个过程可能需要 **10-30分钟**，取决于网络速度
- 请耐心等待，不要中断
- 如果某个包安装失败，可以单独安装：
  ```powershell
  pip install 包名 -i https://pypi.tuna.tsinghua.edu.cn/simple
  ```

### 5.4 验证关键包安装

安装完成后，验证关键包是否安装成功：

```powershell
# 测试导入关键包
python -c "import streamlit; print('✅ Streamlit安装成功')"
python -c "import mlflow; print('✅ MLflow安装成功')"
python -c "import torch; print('✅ PyTorch安装成功')"
python -c "import sqlalchemy; print('✅ SQLAlchemy安装成功')"
python -c "import pandas; print('✅ Pandas安装成功')"
```

**如果所有命令都显示"✅ 安装成功"，说明依赖包安装完成！**

---

## 第六步：创建项目结构

### 6.1 创建目录结构

在项目目录中执行以下命令：

```powershell
# 确保在项目目录中
cd D:\dji_drone_vision_agent

# 创建主应用目录
mkdir app
mkdir app\agents
mkdir app\models
mkdir app\database
mkdir app\utils
mkdir app\web
mkdir app\web\pages

# 创建数据目录
mkdir data
mkdir data\raw
mkdir data\raw\images
mkdir data\raw\annotations
mkdir data\processed
mkdir data\processed\train
mkdir data\processed\val
mkdir data\processed\test
mkdir data\external

# 创建其他目录
mkdir scripts
mkdir database
mkdir outputs
mkdir outputs\models
mkdir outputs\logs
mkdir outputs\reports
mkdir docs
mkdir tests
```

### 6.2 创建__init__.py文件

```powershell
# 创建Python包标识文件
New-Item -ItemType File -Path app\__init__.py
New-Item -ItemType File -Path app\agents\__init__.py
New-Item -ItemType File -Path app\models\__init__.py
New-Item -ItemType File -Path app\database\__init__.py
New-Item -ItemType File -Path app\utils\__init__.py
New-Item -ItemType File -Path app\web\__init__.py
```

### 6.3 验证目录结构

```powershell
# 查看完整的目录树
tree /F /A
```

**应该看到类似这样的结构：**
```
dji_drone_vision_agent/
├── app/
│   ├── agents/
│   ├── models/
│   ├── database/
│   ├── utils/
│   └── web/
├── data/
│   ├── raw/
│   ├── processed/
│   └── external/
├── scripts/
├── database/
├── outputs/
├── docs/
└── tests/
```

---

## 第七步：初始化数据库

### 7.1 创建数据库初始化脚本

在 `scripts` 目录中创建 `setup_database.py` 文件。

**方法：使用PowerShell创建**

```powershell
# 确保在项目目录中
cd D:\dji_drone_vision_agent

# 创建数据库初始化脚本
@"
import sqlite3
from pathlib import Path
import sys

def init_database(db_path=\"database/drone_vision.db\"):
    \"\"\"初始化数据库\"\"\"
    db_file = Path(db_path)
    db_file.parent.mkdir(parents=True, exist_ok=True)
    
    print(f\"📊 初始化数据库: {db_path}\")
    
    conn = sqlite3.connect(str(db_file))
    cursor = conn.cursor()
    
    # 1. 数据版本表
    cursor.execute('''
        CREATE TABLE IF NOT EXISTS data_versions (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            version TEXT UNIQUE NOT NULL,
            data_path TEXT NOT NULL,
            created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
            size_mb REAL,
            description TEXT,
            status TEXT DEFAULT 'active'
        )
    ''')
    
    # 2. 实验记录表
    cursor.execute('''
        CREATE TABLE IF NOT EXISTS experiments (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            experiment_name TEXT NOT NULL,
            run_id TEXT UNIQUE NOT NULL,
            start_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
            end_time TIMESTAMP,
            status TEXT DEFAULT 'running',
            final_accuracy REAL,
            notes TEXT
        )
    ''')
    
    # 3. 模型表
    cursor.execute('''
        CREATE TABLE IF NOT EXISTS models (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            model_name TEXT NOT NULL,
            version TEXT NOT NULL,
            experiment_id INTEGER,
            model_path TEXT NOT NULL,
            accuracy REAL,
            created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
            FOREIGN KEY (experiment_id) REFERENCES experiments(id)
        )
    ''')
    
    # 4. 分析任务表
    cursor.execute('''
        CREATE TABLE IF NOT EXISTS analysis_tasks (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            task_name TEXT NOT NULL,
            image_path TEXT NOT NULL,
            result TEXT,
            status TEXT DEFAULT 'pending',
            created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
            completed_at TIMESTAMP
        )
    ''')
    
    # 5. 用户操作日志表
    cursor.execute('''
        CREATE TABLE IF NOT EXISTS audit_logs (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            user_id TEXT,
            action TEXT NOT NULL,
            resource TEXT,
            timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
            ip_address TEXT,
            result TEXT
        )
    ''')
    
    # 6. 系统配置表
    cursor.execute('''
        CREATE TABLE IF NOT EXISTS system_config (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            config_key TEXT UNIQUE NOT NULL,
            config_value TEXT,
            updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
        )
    ''')
    
    # 插入默认配置
    cursor.execute('''
        INSERT OR IGNORE INTO system_config (config_key, config_value)
        VALUES ('system_version', '1.0.0')
    ''')
    
    conn.commit()
    conn.close()
    
    print(\"✅ 数据库初始化完成！\")
    print(f\"   数据库位置: {db_file.absolute()}\")
    print(\"   已创建表: data_versions, experiments, models, analysis_tasks, audit_logs, system_config\")

if __name__ == \"__main__\":
    db_path = sys.argv[1] if len(sys.argv) > 1 else \"database/drone_vision.db\"
    init_database(db_path)
"@ | Out-File -FilePath scripts\setup_database.py -Encoding utf8
```

### 7.2 运行数据库初始化

```powershell
# 确保虚拟环境已激活
.\venv\Scripts\activate

# 运行数据库初始化脚本
python scripts\setup_database.py
```

**预期输出：**
```
📊 初始化数据库: database/drone_vision.db
✅ 数据库初始化完成！
   数据库位置: D:\dji_drone_vision_agent\database\drone_vision.db
   已创建表: data_versions, experiments, models, analysis_tasks, audit_logs, system_config
```

### 7.3 验证数据库创建

```powershell
# 检查数据库文件是否存在
Test-Path database\drone_vision.db
```

**应该返回：`True`**

---

## 第八步：配置系统

### 8.1 创建配置文件

在项目根目录创建 `config.yaml` 文件：

```powershell
# 创建配置文件
@"
# 大疆无人机视觉智能Agent系统配置文件
# 海南博悦科技有限公司

system:
  name: \"大疆无人机视觉智能Agent系统\"
  version: \"1.0.0\"
  company: \"海南博悦科技有限公司\"

database:
  type: \"sqlite\"
  sqlite_path: \"database/drone_vision.db\"

mlflow:
  tracking_uri: \"file:./mlruns\"
  experiment_name: \"dji_drone_vision\"

model:
  default_model_path: \"outputs/models/drone_vision_model.pth\"
  num_classes: 5
  image_size: [640, 640]
  class_names:
    - \"建筑物\"
    - \"道路\"
    - \"植被\"
    - \"水体\"
    - \"车辆\"

paths:
  raw_data: \"data/raw\"
  processed_data: \"data/processed\"
  output_dir: \"outputs\"
"@ | Out-File -FilePath config.yaml -Encoding utf8
```

### 8.2 验证配置文件

```powershell
# 测试配置文件是否有效
python -c "import yaml; yaml.safe_load(open('config.yaml')); print('✅ 配置文件有效')"
```

---

## 第九步：启动系统

### 9.1 创建启动脚本

在 `scripts` 目录创建 `start_system.py`：

```powershell
# 创建启动脚本
@"
import subprocess
import time
import webbrowser
from pathlib import Path
import sys

def check_dependencies():
    \"\"\"检查必要的依赖\"\"\"
    print(\"🔍 检查系统依赖...\")
    required_packages = ['streamlit', 'mlflow', 'torch', 'sqlalchemy']
    missing_packages = []
    
    for package in required_packages:
        try:
            __import__(package)
            print(f\"  ✅ {package}\")
        except ImportError:
            print(f\"  ❌ {package} 未安装\")
            missing_packages.append(package)
    
    if missing_packages:
        print(f\"\n⚠️  缺少以下依赖包: {', '.join(missing_packages)}\")
        print(\"请运行: pip install -r requirements_agent.txt\")
        return False
    
    return True

def init_database():
    \"\"\"初始化数据库\"\"\"
    print(\"📊 初始化数据库...\")
    try:
        from scripts.setup_database import init_database as db_init
        db_init()
        return True
    except Exception as e:
        print(f\"  ⚠️  数据库初始化失败: {e}\")
        return True

def start_streamlit():
    \"\"\"启动Streamlit Web界面\"\"\"
    print(\"\n🚀 启动Web界面...\")
    streamlit_app = Path(\"app/web/streamlit_app_simple.py\")
    
    if not streamlit_app.exists():
        print(f\"  ❌ 找不到Streamlit应用: {streamlit_app}\")
        print(\"  请确保文件存在\")
        return None
    
    try:
        process = subprocess.Popen(
            [sys.executable, \"-m\", \"streamlit\", \"run\",
             str(streamlit_app), \"--server.port\", \"8501\"],
            cwd=Path.cwd()
        )
        print(\"  ✅ Streamlit已启动\")
        return process
    except Exception as e:
        print(f\"  ❌ 启动失败: {e}\")
        return None

def main():
    print(\"=\" * 60)
    print(\"🚁 大疆无人机视觉智能Agent系统\")
    print(\"   海南博悦科技有限公司\")
    print(\"=\" * 60)
    print()
    
    if not check_dependencies():
        return
    
    init_database()
    
    streamlit_process = start_streamlit()
    if not streamlit_process:
        return
    
    print(\"\n⏳ 等待服务启动...\")
    time.sleep(3)
    
    try:
        webbrowser.open(\"http://localhost:8501\")
        print(\"  ✅ 已自动打开浏览器\")
    except:
        print(\"  ⚠️  无法自动打开浏览器，请手动访问: http://localhost:8501\")
    
    print(\"\n\" + \"=\" * 60)
    print(\"✅ 系统启动成功！\")
    print(\"=\" * 60)
    print(\"\n📱 访问地址: http://localhost:8501\")
    print(\"\n⚠️  按 Ctrl+C 停止服务\")
    
    try:
        streamlit_process.wait()
    except KeyboardInterrupt:
        print(\"\n\n正在关闭服务...\")
        streamlit_process.terminate()
        streamlit_process.wait()
        print(\"✅ 所有服务已停止\")

if __name__ == \"__main__\":
    main()
"@ | Out-File -FilePath scripts\start_system.py -Encoding utf8
```

### 9.2 创建简化版Web应用

在 `app/web` 目录创建 `streamlit_app_simple.py`：

**⚠️ 这个文件比较大，建议：**
1. 从之前的文件复制 `app/web/streamlit_app_simple.py`
2. 或者使用我已经创建的版本

**验证文件是否存在：**
```powershell
Test-Path app\web\streamlit_app_simple.py
```

**如果不存在，需要创建这个文件**（内容较长，建议从之前的文件复制）

### 9.3 启动系统

```powershell
# 确保虚拟环境已激活
.\venv\Scripts\activate

# 确保在项目根目录
cd D:\dji_drone_vision_agent

# 启动系统
python scripts\start_system.py
```

**预期输出：**
```
============================================================
🚁 大疆无人机视觉智能Agent系统
   海南博悦科技有限公司
============================================================

🔍 检查系统依赖...
  ✅ streamlit
  ✅ mlflow
  ✅ torch
  ✅ sqlalchemy
📊 初始化数据库...
📊 初始化数据库: database/drone_vision.db
✅ 数据库初始化完成！

🚀 启动Web界面...
  ✅ Streamlit已启动

⏳ 等待服务启动...

============================================================
✅ 系统启动成功！
============================================================

📱 访问地址: http://localhost:8501

⚠️  按 Ctrl+C 停止服务
```

**浏览器应该会自动打开，显示Web界面。**

---

## 第十步：验证系统

### 10.1 检查Web界面

1. **浏览器应该自动打开**，显示系统界面
2. **如果没有自动打开**，手动访问：`http://localhost:8501`
3. **应该看到**：
   - 标题："🚁 大疆无人机视觉智能Agent系统"
   - 左侧导航菜单
   - 系统概览页面

### 10.2 测试功能

1. **系统概览**
   - 查看关键指标是否显示
   - 检查是否有数据统计图表

2. **数据管理**
   - 点击左侧菜单"📊 数据管理"
   - 尝试添加一个新数据版本

3. **实验跟踪**
   - 点击"🔬 实验跟踪"
   - 创建一个新实验

4. **智能分析**
   - 点击"🤖 智能分析"
   - 上传一张测试图像

### 10.3 验证数据库

```powershell
# 检查数据库文件
python -c "import sqlite3; conn = sqlite3.connect('database/drone_vision.db'); cursor = conn.cursor(); cursor.execute('SELECT COUNT(*) FROM experiments'); print('实验数量:', cursor.fetchone()[0]); conn.close()"
```

---

## 🐛 常见问题解决

### 问题1：Python命令找不到

**症状**：`python --version` 显示错误

**解决方法**：
1. 检查Python是否已安装
2. 检查是否添加到PATH环境变量
3. 尝试使用 `py` 命令：`py --version`
4. 如果使用 `py` 可以，后续都用 `py` 替代 `python`

### 问题2：pip安装包很慢或失败

**症状**：`pip install` 速度很慢或超时

**解决方法**：
```powershell
# 使用国内镜像源
pip install -r requirements_agent.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 或者使用阿里云镜像
pip install -r requirements_agent.txt -i https://mirrors.aliyun.com/pypi/simple/
```

### 问题3：虚拟环境激活失败

**症状**：`.\venv\Scripts\activate` 报错

**解决方法**：
```powershell
# 检查PowerShell执行策略
Get-ExecutionPolicy

# 如果显示Restricted，需要修改策略
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# 然后重新激活
.\venv\Scripts\activate
```

### 问题4：端口8501被占用

**症状**：启动Streamlit时提示端口被占用

**解决方法**：
```powershell
# 方法1：使用其他端口
streamlit run app\web\streamlit_app_simple.py --server.port 8502

# 方法2：关闭占用端口的进程
# 查找占用端口的进程
netstat -ano | findstr :8501
# 找到PID后，关闭进程（替换<PID>为实际PID）
taskkill /PID <PID> /F
```

### 问题5：模块导入错误

**症状**：`ModuleNotFoundError: No module named 'xxx'`

**解决方法**：
```powershell
# 确保虚拟环境已激活
.\venv\Scripts\activate

# 重新安装缺失的包
pip install xxx -i https://pypi.tuna.tsinghua.edu.cn/simple

# 或者重新安装所有依赖
pip install -r requirements_agent.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 问题6：数据库文件找不到

**症状**：数据库初始化失败

**解决方法**：
```powershell
# 确保在项目根目录
cd D:\dji_drone_vision_agent

# 手动创建database目录
mkdir database

# 重新运行初始化
python scripts\setup_database.py
```

### 问题7：Streamlit应用找不到

**症状**：启动脚本找不到 `streamlit_app_simple.py`

**解决方法**：
1. 检查文件是否存在：
   ```powershell
   Test-Path app\web\streamlit_app_simple.py
   ```
2. 如果不存在，需要创建这个文件（从之前的文件复制）
3. 或者直接运行Streamlit：
   ```powershell
   streamlit run app\web\streamlit_app_simple.py
   ```

### 问题8：PyTorch安装失败

**症状**：`pip install torch` 失败或很慢

**解决方法**：
```powershell
# 使用PyTorch官方源（推荐）
pip install torch torchvision -i https://download.pytorch.org/whl/torch_stable.html

# 或者使用CPU版本（更小更快）
pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
```

---

## ✅ 安装完成检查清单

完成所有步骤后，请确认：

- [ ] ✅ Python已安装并可以运行
- [ ] ✅ 虚拟环境已创建并激活
- [ ] ✅ 所有依赖包已安装
- [ ] ✅ 项目目录结构已创建
- [ ] ✅ 数据库已初始化
- [ ] ✅ 配置文件已创建
- [ ] ✅ Web应用文件已创建
- [ ] ✅ 系统可以启动
- [ ] ✅ Web界面可以访问
- [ ] ✅ 各个功能页面可以打开

---

## 🎉 恭喜！

如果你完成了以上所有步骤，恭喜你！**大疆无人机视觉智能Agent系统已经成功安装并运行！**

### 下一步

1. **熟悉系统**：浏览各个功能页面
2. **阅读文档**：查看完整的设计方案文档
3. **学习代码**：阅读代码文件，理解实现逻辑
4. **扩展功能**：尝试添加新功能或修改界面

### 常用命令速查

```powershell
# 激活虚拟环境
.\venv\Scripts\activate

# 启动系统
python scripts\start_system.py

# 直接启动Streamlit
streamlit run app\web\streamlit_app_simple.py

# 初始化数据库
python scripts\setup_database.py

# 安装新包
pip install 包名 -i https://pypi.tuna.tsinghua.edu.cn/simple
```

---

## 📞 获取帮助

如果遇到问题：

1. **查看错误信息**：仔细阅读PowerShell的错误输出
2. **检查文档**：回顾本文档的相关步骤
3. **验证环境**：确认Python、pip、虚拟环境都正常
4. **联系支持**：联系项目导师或技术支持

---

**祝您使用愉快！** 🚁✨

*海南博悦科技有限公司 - 大疆无人机视觉智能Agent系统*

