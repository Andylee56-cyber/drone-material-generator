# 第6周工作执行检查清单

> **使用说明**: 按照步骤逐一完成，每完成一项打勾 ✅，遇到问题参考《第6周工作部署文档_完整版.md》

---

## 📅 Day 1: 环境准备

### 上午：环境检查与配置

#### 1.1 打开PowerShell并进入项目目录
**执行命令**:
```powershell
cd D:\mlflow_learning_project
pwd
```
- [ ] **1.1** 已进入项目目录，显示 `D:\mlflow_learning_project`

#### 1.2 创建所有必需目录
**执行命令**:
```powershell
New-Item -ItemType Directory -Path "agents","app\web","scripts","reports","temp_uploads","test_images","test_images\batch","test_results","real_data_test","real_data_results","final_report" -Force | Out-Null
Get-ChildItem -Directory | Select-Object Name
```
- [ ] **1.2** 所有目录创建成功

#### 1.3 激活Conda环境
**执行命令**:
```powershell
conda activate uav_adv
python --version
```
- [ ] **1.3** 环境激活成功，Python版本 >= 3.10

#### 1.4 安装依赖包
**执行命令**:
```powershell
pip install ultralytics==8.2.74
pip install plotly>=5.23.0
pip install streamlit>=1.37.1
python -c "import ultralytics, plotly, streamlit; print('✅ 所有依赖OK')"
```
- [ ] **1.4** 所有依赖包安装成功

#### 1.5 验证YOLO模型
**执行命令**:
```powershell
python -c "from ultralytics import YOLO; model = YOLO('yolov8n.pt'); print('✅ YOLO模型OK')"
```
- [ ] **1.5** YOLO模型加载成功

### 下午：代码结构验证

#### 2.1 检查核心文件
**执行命令**:
```powershell
$files = @("agents\__init__.py","agents\image_quality_analyzer.py","agents\material_generator_agent.py","app\web\material_analyzer_app.py","scripts\batch_analyze.py","scripts\generate_report.py"); $files | ForEach-Object { if (Test-Path $_) { Write-Host "✅ $_" -ForegroundColor Green } else { Write-Host "❌ $_" -ForegroundColor Red } }
```
- [ ] **2.1** 所有核心文件都存在

#### 2.2 验证模块导入
**执行命令**:
```powershell
python -c "from agents.image_quality_analyzer import ImageQualityAnalyzer; from agents.material_generator_agent import MaterialGeneratorAgent; print('✅ 模块导入成功')"
```
- [ ] **2.2** 模块导入成功

**今日目标**: 环境配置完成，所有依赖正常 ✅

---

## 📅 Day 2: 单图分析测试

#### 3.1 准备测试图片
**执行命令**:
```powershell
# 方法1: 复制现有图片
Copy-Item "D:\你的图片路径\*.jpg" -Destination "test_images\test_single.jpg" -ErrorAction SilentlyContinue

# 方法2: 打开目录手动复制
explorer test_images

# 验证图片存在
Get-ChildItem test_images\*.jpg | Select-Object -First 1
```
- [ ] **3.1** 测试图片已准备（至少1张）

#### 3.2 执行单图分析测试
**执行命令**:
```powershell
$img = Get-ChildItem test_images\*.jpg | Select-Object -First 1
python -c "import sys; sys.path.insert(0, '.'); from agents.image_quality_analyzer import ImageQualityAnalyzer; a = ImageQualityAnalyzer(); r = a.analyze_single_image(r'$($img.FullName)'); [print(f'{k}: {v:.2f}%') for k, v in r.items()]"
```
- [ ] **3.2** 单图分析执行成功，显示8个维度分数

#### 3.3 验证分析结果
**执行命令**:
```powershell
python -c "import sys; sys.path.insert(0, '.'); from agents.image_quality_analyzer import ImageQualityAnalyzer; a = ImageQualityAnalyzer(); r = a.analyze_single_image('test_images/test_single.jpg'); print('维度数量:', len(r)); print('分数范围:', min(r.values()), '-', max(r.values())); print('✅ 结果验证通过' if len(r)==8 and all(0<=v<=100 for v in r.values()) else '❌ 结果异常')"
```
- [ ] **3.3** 验证通过：8个维度，分数0-100

**今日目标**: 单图分析功能正常工作 ✅

---

## 📅 Day 3: 批量分析测试

#### 4.1 准备批量测试图片
**执行命令**:
```powershell
# 复制单张图片多份用于测试
if (Test-Path "test_images\test_single.jpg") {
    1..5 | ForEach-Object { Copy-Item "test_images\test_single.jpg" -Destination "test_images\batch\test_$_.jpg" -Force }
}
# 或从其他目录复制
# Copy-Item "D:\无人机素材\*.jpg" -Destination "test_images\batch\" -ErrorAction SilentlyContinue

# 检查数量
(Get-ChildItem "test_images\batch\*.jpg").Count
```
- [ ] **4.1** 批量测试图片已准备（5-10张）

#### 4.2 执行批量分析测试
**执行命令**:
```powershell
python scripts\batch_analyze.py --input-dir "test_images\batch" --output-dir "test_results" --min-quality 70
```
- [ ] **4.2** 批量分析执行成功，生成结果文件

#### 4.3 验证批量分析结果
**执行命令**:
```powershell
Get-ChildItem test_results\*.json | ForEach-Object { Write-Host "✅ $($_.Name)" -ForegroundColor Green }
python -c "import json; from pathlib import Path; f = list(Path('test_results').glob('analysis_result_*.json'))[0]; d = json.load(open(f, encoding='utf-8')); print(f'总图片数: {d[\"analysis\"][\"total_images\"]}'); print('✅ 结果验证通过')"
```
- [ ] **4.3** 验证通过：结果文件存在，数据完整

**今日目标**: 批量分析功能正常工作 ✅

---

## 📅 Day 4: Web界面测试

#### 5.1 启动Web界面
**执行命令**:
```powershell
# 方式1: 使用启动脚本
.\start_material_analyzer.ps1

# 方式2: 手动启动
conda activate uav_adv
streamlit run app\web\material_analyzer_app.py --server.port 8502

# 打开浏览器
start http://localhost:8502
```
- [ ] **5.1** Web界面启动成功，浏览器能访问 http://localhost:8502

#### 5.2 测试单图分析功能
**操作步骤**:
1. 在浏览器中点击"单图分析"标签
2. 点击"上传图片"，选择 `test_images\test_single.jpg`
3. 点击"开始分析"按钮
4. 检查是否显示雷达图和得分表
- [ ] **5.2** 单图分析功能正常：雷达图显示，8个维度都有分数

#### 5.3 测试批量分析功能
**操作步骤**:
1. 点击"批量分析"标签
2. 上传 `test_images\batch\` 目录下的所有图片
3. 点击"开始批量分析"
4. 检查是否显示平均雷达图、质量表格、改进建议
- [ ] **5.3** 批量分析功能正常：显示平均雷达图、质量表格、改进建议

#### 5.4 测试报告生成功能
**操作步骤**:
1. 点击"结果报告"标签
2. 点击"生成详细报告"按钮
3. 点击"下载报告"按钮
4. 检查下载的JSON文件内容
- [ ] **5.4** 报告生成功能正常：能下载JSON报告

**今日目标**: Web界面所有功能正常 ✅

---

## 📅 Day 5-6: 真实数据验证和素材生成

### 真实数据分析
- [ ] **6.1** 准备真实无人机素材（20-50张不同质量的图片）
- [ ] **6.2** 执行真实数据批量分析
- [ ] **6.3** 验证分析准确性（人工对比，准确率>85%）

### 批量素材生成
#### 6.4 准备源素材目录
**执行命令**:
```powershell
# 创建源素材目录
New-Item -ItemType Directory -Path "source_materials" -Force | Out-Null
explorer source_materials
# 复制50-200张待筛选的无人机图片到此目录
```
- [ ] **6.4** 源素材目录已准备，包含50-200张图片

#### 6.5 执行批量素材生成
**执行命令**:
```powershell
python scripts\generate_materials.py --source-dir "source_materials" --output-dir "generated_materials" --min-quality 75.0 --max-count 50 --generate-report
```
- [ ] **6.5** 批量素材生成成功，生成高质量素材

#### 6.6 验证生成结果
**执行命令**:
```powershell
Get-ChildItem "generated_materials\high_quality_*.jpg" | Measure-Object | Select-Object -ExpandProperty Count
python -c "import json; from pathlib import Path; f = list(Path('generated_materials').glob('material_metadata_*.json'))[0]; d = json.load(open(f, encoding='utf-8')); print(f'生成素材数: {d[\"generated_count\"]}')"
```
- [ ] **6.6** 验证通过：生成素材数量正确，质量达标

**今日目标**: 真实数据分析完成，批量素材生成功能正常 ✅

---

## 📅 Day 6 下午: 性能优化

- [ ] **7.1** 测试分析速度（单图<3秒，批量20张<60秒）
- [ ] **7.2** 检查GPU使用情况（如果可用）
- [ ] **7.3** 根据测试结果进行优化

**今日目标**: 性能指标达标

---

## 📅 Day 7: 最终报告

- [ ] **8.1** 整理所有测试结果
- [ ] **8.2** 生成最终总结报告
- [ ] **8.3** 准备演示材料（截图、样例数据）

**今日目标**: 所有交付物准备完成

---

## ✅ 最终验收检查

### 功能验收
- [ ] 单图8维度分析 ✅
- [ ] 批量图片分析 ✅
- [ ] 8维度雷达图 ✅
- [ ] 质量评估和筛选 ✅
- [ ] 改进建议生成 ✅
- [ ] 报告导出功能 ✅
- [ ] **批量生成高质量素材** ✅
- [ ] **素材生成报告** ✅

### 性能验收
- [ ] 单图分析 < 3秒 ✅
- [ ] 批量分析(20张) < 60秒 ✅
- [ ] 分析准确率 > 85% ✅

### 交付物验收
- [ ] 所有代码文件 ✅
- [ ] 部署文档 ✅
- [ ] 测试结果 ✅
- [ ] 项目总结报告 ✅

---

## 📝 问题记录

在完成过程中遇到的问题和解决方案：

| 日期 | 问题描述 | 解决方案 | 状态 |
|------|----------|----------|------|
|      |          |          |      |
|      |          |          |      |

---

## 💡 每日总结

### Day 1 总结
- 完成情况: 
- 遇到的问题: 
- 明日计划: 

### Day 2 总结
- 完成情况: 
- 遇到的问题: 
- 明日计划: 

### Day 3 总结
- 完成情况: 
- 遇到的问题: 
- 明日计划: 

### Day 4 总结
- 完成情况: 
- 遇到的问题: 
- 明日计划: 

### Day 5-6 总结
- 完成情况: 
- 遇到的问题: 
- 明日计划: 

### Day 7 总结
- 完成情况: 
- 最终成果: 

---

**使用提示**:
1. 每天开始工作前，查看当天的任务清单
2. 每完成一项任务，立即打勾 ✅
3. 遇到问题及时记录，并参考详细文档
4. 每天结束时，填写每日总结
5. 所有任务完成后，进行最终验收检查

**祝你顺利完成第6周工作！** 🎉

