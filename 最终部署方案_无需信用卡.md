# 🚀 最终部署方案（无需信用卡）

## 📋 问题说明

- Railway：免费版只能部署数据库
- Render：需要信用卡（银联卡不支持）
- Fly.io：需要信用卡验证

## 🎯 推荐方案

### 方案一：Streamlit Cloud（完全免费，推荐⭐⭐⭐⭐）

#### 优点
- ✅ **完全免费**：无需信用卡
- ✅ **简单易用**：GitHub 一键部署
- ✅ **已配置好**：你的代码已经可以部署

#### 缺点
- ⚠️ **会 sleep**：免费版在无活动时会 sleep
- ⚠️ **首次访问慢**：sleep 后首次访问需等待约 30 秒

#### 部署步骤

1. **访问 Streamlit Cloud**
   - 打开：https://share.streamlit.io
   - 使用 GitHub 账号登录

2. **部署应用**
   - 点击 "New app"
   - 选择仓库：`Andylee56-cyber/drone-material-generator`
   - Main file path: `app/web/material_generator_app.py`
   - 点击 "Deploy"

3. **等待部署**
   - 约 2-3 分钟
   - 部署完成后会生成 URL

4. **获取链接**
   - 格式：`https://xxx.streamlit.app`
   - 复制链接即可访问

#### 给老板的说明

如果老板访问时遇到 sleep：
- 告诉老板：首次访问需要等待约 30 秒（免费版限制）
- 或者：你可以定期访问保持活跃（每 15 分钟访问一次）

---

### 方案二：国内云服务器（最稳定，推荐⭐⭐⭐⭐⭐）

#### 优点
- ✅ **最稳定**：24/7 运行，不会 sleep
- ✅ **速度快**：国内访问速度最快
- ✅ **专业**：适合给老板展示
- ✅ **价格便宜**：¥24-50/月

#### 缺点
- ⚠️ **需要付费**：但价格便宜

#### 推荐服务商

**1. 阿里云轻量应用服务器**
- 价格：¥24/月起
- 配置：2核2G，3M带宽
- 访问：https://www.aliyun.com

**2. 腾讯云轻量应用服务器**
- 价格：¥24/月起
- 配置：2核2G，3M带宽
- 访问：https://cloud.tencent.com

#### 部署步骤（以阿里云为例）

1. **购买服务器**
   - 选择：轻量应用服务器
   - 配置：2核2G，3M带宽
   - 系统：Ubuntu 22.04
   - 地域：选择离你最近的

2. **连接服务器**
   ```bash
   # 使用 SSH 连接
   ssh root@你的服务器IP
   ```

3. **安装依赖**
   ```bash
   # 更新系统
   apt update && apt upgrade -y
   
   # 安装 Python 3.11
   apt install python3.11 python3.11-venv python3-pip git -y
   ```

4. **克隆项目**
   ```bash
   cd /opt
   git clone https://github.com/Andylee56-cyber/drone-material-generator.git
   cd drone-material-generator
   ```

5. **安装依赖**
   ```bash
   python3.11 -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
   ```

6. **配置 Streamlit**
   ```bash
   mkdir -p ~/.streamlit
   cat > ~/.streamlit/config.toml << EOF
   [server]
   port = 8501
   address = "0.0.0.0"
   headless = true
   enableCORS = false
   enableXsrfProtection = true
   
   [browser]
   gatherUsageStats = false
   EOF
   ```

7. **创建系统服务（确保 24/7 运行）**
   ```bash
   sudo cat > /etc/systemd/system/streamlit-app.service << EOF
   [Unit]
   Description=Streamlit App
   After=network.target
   
   [Service]
   Type=simple
   User=root
   WorkingDirectory=/opt/drone-material-generator
   Environment="PATH=/opt/drone-material-generator/venv/bin"
   ExecStart=/opt/drone-material-generator/venv/bin/streamlit run app/web/material_generator_app.py --server.port 8501 --server.address 0.0.0.0
   Restart=always
   RestartSec=10
   
   [Install]
   WantedBy=multi-user.target
   EOF
   
   # 启动服务
   sudo systemctl daemon-reload
   sudo systemctl enable streamlit-app
   sudo systemctl start streamlit-app
   ```

8. **开放端口**
   ```bash
   # 开放 8501 端口
   sudo ufw allow 8501/tcp
   sudo ufw reload
   ```

9. **访问应用**
   - 在浏览器访问：`http://你的服务器IP:8501`

---

## 🎯 方案对比

| 方案 | 费用 | 稳定性 | 速度 | 适合场景 |
|------|------|--------|------|----------|
| **Streamlit Cloud** | 免费 | ⭐⭐⭐ | ⭐⭐⭐ | 临时展示、测试 |
| **国内云服务器** | ¥24-50/月 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 正式展示、长期使用 |

---

## 💡 我的建议

### 如果只是临时给老板展示
→ **使用 Streamlit Cloud**（免费，虽然会 sleep，但够用）

### 如果需要正式、稳定展示
→ **使用国内云服务器**（¥24/月，最稳定专业）

---

## 🚀 立即开始

### 方案一：Streamlit Cloud（5分钟完成）

1. 访问：https://share.streamlit.io
2. 登录 GitHub
3. 点击 "New app"
4. 选择仓库：`Andylee56-cyber/drone-material-generator`
5. Main file: `app/web/material_generator_app.py`
6. 点击 "Deploy"
7. 等待完成，获取链接

### 方案二：国内云服务器（需要购买，但最稳定）

1. 访问阿里云或腾讯云
2. 购买轻量服务器（¥24/月起）
3. 按照上面的步骤部署

---

## 📝 总结

**推荐顺序**：
1. **Streamlit Cloud**（免费，快速，适合临时展示）
2. **国内云服务器**（付费但便宜，最稳定专业）

选择哪个方案取决于你的需求：
- 临时展示 → Streamlit Cloud
- 正式展示 → 国内云服务器

