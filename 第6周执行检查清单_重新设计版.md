# 第6周工作执行检查清单 - 重新设计版
## 单图输入→多角度生成→8维雷达图分析

> **核心功能**: 输入一张图片，自动生成多角度素材，并生成8维度雷达图可视化

---

## 📅 Day 1: 环境准备和目录创建

### 1.1 进入项目目录
**执行命令**:
```powershell
cd D:\mlflow_learning_project
pwd
```
- [ ] **1.1** 已进入项目目录

### 1.2 创建目录结构
**执行命令**:
```powershell
New-Item -ItemType Directory -Path "agents","app\web","scripts","reports","temp_uploads","test_images","generated_materials" -Force | Out-Null
Get-ChildItem -Directory | Select-Object Name
```
- [ ] **1.2** 所有目录创建成功

### 1.3 激活环境并安装依赖
**执行命令**:
```powershell
conda activate uav_adv
pip install ultralytics==8.2.74 plotly>=5.23.0 streamlit>=1.37.1 opencv-python-headless==4.9.0.80 pillow>=9.5.0
python -c "import ultralytics, plotly, streamlit, cv2; print('✅ 依赖OK')"
```
- [ ] **1.3** 环境配置完成

**今日目标**: 环境配置完成，所有依赖正常 ✅

---

## 📅 Day 2: 多角度生成功能测试

### 2.1 准备测试图片
**执行命令**:
```powershell
# 从你的素材目录复制一张图片
$sourceImage = Get-ChildItem "D:\网上素材图片\*.jpg" | Select-Object -First 1
Copy-Item $sourceImage.FullName -Destination "test_images\test_input.jpg" -Force
Get-Item "test_images\test_input.jpg"
```
- [ ] **2.1** 测试图片已准备

### 2.2 测试多角度生成
**执行命令**:
```powershell
python -c "
import sys
sys.path.insert(0, '.')
from agents.image_multi_angle_generator import ImageMultiAngleGenerator

generator = ImageMultiAngleGenerator()
result = generator.generate_multi_angle_images(
    input_image_path='test_images/test_input.jpg',
    output_dir='test_generated',
    num_generations=8
)
print(f'✅ 生成 {result[\"num_generated\"]} 张图片')
"
```
- [ ] **2.2** 多角度生成成功，生成8张图片

### 2.3 查看生成的图片
**执行命令**:
```powershell
Get-ChildItem "test_generated\generated_*.jpg" | Select-Object Name
explorer test_generated
```
- [ ] **2.3** 生成的图片可以正常查看

**今日目标**: 多角度生成功能正常工作 ✅

---

## 📅 Day 3: 完整流程测试（生成+分析）

### 3.1 执行完整流程
**执行命令**:
```powershell
python scripts\generate_from_single_image.py --input-image "test_images\test_input.jpg" --output-dir "test_complete" --num-generations 8 --analyze
```
- [ ] **3.1** 完整流程执行成功

### 3.2 验证分析结果
**执行命令**:
```powershell
python -c "
import json
from pathlib import Path
f = list(Path('test_complete').glob('analysis_result_*.json'))[0]
d = json.load(open(f, encoding='utf-8'))
print(f'分析图片数: {d[\"analysis\"][\"total_images\"]}')
print(f'整体质量: {d[\"recommendations\"][\"overall_quality\"]:.2f}%')
print('✅ 分析结果验证通过')
"
```
- [ ] **3.2** 分析结果正确，包含8个维度数据

**今日目标**: 完整流程（生成+分析）正常工作 ✅

---

## 📅 Day 4: Web界面测试

### 4.1 启动Web界面
**执行命令**:
```powershell
streamlit run app\web\material_generator_app.py --server.port 8502
start http://localhost:8502
```
- [ ] **4.1** Web界面启动成功

### 4.2 测试完整功能
**操作步骤**:
1. 上传测试图片
2. 设置生成数量为8
3. 勾选"生成后自动分析"
4. 点击"生成多角度素材并分析"
5. 等待处理完成
- [ ] **4.2** 功能测试通过

### 4.3 验证雷达图显示
**检查项**:
- [ ] 雷达图是圆形/多边形（不是条形图）
- [ ] 显示8个维度
- [ ] 每个维度都有数值
- [ ] 图表有颜色填充
- [ ] 显示图例
- [ ] **4.3** 雷达图显示正确

**今日目标**: Web界面所有功能正常，雷达图正确显示 ✅

---

## 📅 Day 5-6: 真实数据测试和优化

### 5.1 使用真实图片测试
**执行命令**:
```powershell
# 使用真实的无人机图片
$realImage = Get-ChildItem "D:\网上素材图片\*.jpg" | Select-Object -First 1
Copy-Item $realImage.FullName -Destination "test_images\real_input.jpg" -Force

# 在Web界面中测试
# 或使用命令行
python scripts\generate_from_single_image.py --input-image "test_images\real_input.jpg" --output-dir "real_output" --num-generations 12 --analyze
```
- [ ] **5.1** 真实数据测试完成

### 5.2 性能优化
**检查项**:
- [ ] 生成速度 < 10秒（8张）
- [ ] 分析速度 < 60秒（8张）
- [ ] 雷达图加载 < 2秒
- [ ] **5.2** 性能达标

**今日目标**: 真实数据测试完成，性能优化完成 ✅

---

## ✅ 最终验收检查

### 功能验收
- [ ] 能够上传单张图片 ✅
- [ ] 能够生成多角度素材（至少8张） ✅
- [ ] 能够自动进行8维度分析 ✅
- [ ] **能够显示8维度雷达图（圆形图表）** ✅
- [ ] 雷达图显示所有8个维度 ✅
- [ ] 能够查看详细得分表格 ✅
- [ ] 能够查看质量评估表格 ✅

### 性能验收
- [ ] 生成8张图片 < 10秒 ✅
- [ ] 分析8张图片 < 60秒 ✅
- [ ] 雷达图加载 < 2秒 ✅

### 质量验收
- [ ] 生成的图片质量良好 ✅
- [ ] 雷达图美观清晰 ✅
- [ ] 数据准确可靠 ✅

---

**使用提示**:
1. 每天开始工作前，查看当天的任务清单
2. 每完成一项任务，立即打勾 ✅
3. 遇到问题及时记录，并参考详细文档
4. 所有任务完成后，进行最终验收检查

**祝你顺利完成第6周工作！** 🎉

