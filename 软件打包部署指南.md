# 🚁 无人机素材生成系统 - 软件打包部署指南

## 📋 目录
1. [方案选择](#方案选择)
2. [方案一：PyInstaller打包（推荐）](#方案一pyinstaller打包推荐)
3. [方案二：便携式启动包](#方案二便携式启动包)
4. [方案三：一键启动脚本](#方案三一键启动脚本)
5. [测试与演示准备](#测试与演示准备)

---

## 方案选择

### 方案对比

| 方案 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| **PyInstaller打包** | 完全独立，无需安装Python | 文件较大（2-3GB），首次启动慢 | 正式演示、分发 |
| **便携式启动包** | 文件小，启动快 | 需要目标机器有Python环境 | 内部测试、开发环境 |
| **一键启动脚本** | 最简单，快速演示 | 需要安装依赖 | 快速演示、开发调试 |

**推荐：方案一（PyInstaller）** - 最适合给老板演示

---

## 方案一：PyInstaller打包（推荐）

### 第一步：安装PyInstaller

```powershell
# 激活conda环境
conda activate uav_advupdfa

# 安装PyInstaller
pip install pyinstaller
```

### 第二步：创建打包配置文件

创建 `build_app.spec` 文件（用于自定义打包配置）：

```powershell
@"
# -*- mode: python ; coding: utf-8 -*-

block_cipher = None

a = Analysis(
    ['app/web/material_generator_app.py'],
    pathex=[],
    binaries=[],
    datas=[
        ('agents', 'agents'),
        ('configs', 'configs'),
    ],
    hiddenimports=[
        'streamlit',
        'plotly',
        'pandas',
        'numpy',
        'PIL',
        'cv2',
        'torch',
        'ultralytics',
        'pathlib',
    ],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    win_no_prefer_redirects=False,
    win_private_assemblies=False,
    cipher=block_cipher,
    noarchive=False,
)

pyz = PYZ(a.pure, a.zipped_data, cipher=block_cipher)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.zipfiles,
    a.datas,
    [],
    name='无人机素材生成系统',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon=None,  # 可以添加图标文件路径
)
"@ | Set-Content "build_app.spec" -Encoding UTF8
```

### 第三步：创建启动脚本（包装Streamlit）

创建 `launcher.py`（用于启动Streamlit应用）：

```powershell
@"
"""
无人机素材生成系统 - 启动器
"""
import sys
import subprocess
import webbrowser
import time
from pathlib import Path

def main():
    print("=" * 60)
    print("🚁 无人机素材多角度生成与分析系统")
    print("=" * 60)
    print("\n正在启动系统，请稍候...\n")
    
    # 获取应用路径
    if getattr(sys, 'frozen', False):
        # 如果是打包后的exe
        app_dir = Path(sys.executable).parent
        app_path = app_dir / "material_generator_app.py"
    else:
        # 如果是开发环境
        app_dir = Path(__file__).parent.parent
        app_path = app_dir / "app" / "web" / "material_generator_app.py"
    
    if not app_path.exists():
        print(f"❌ 错误：找不到应用文件 {app_path}")
        input("按回车键退出...")
        return
    
    try:
        # 启动Streamlit
        print(f"📂 应用路径: {app_path}")
        print("🌐 正在启动Web服务器...\n")
        
        process = subprocess.Popen(
            [sys.executable, "-m", "streamlit", "run", str(app_path), 
             "--server.port", "8501", "--server.headless", "true"],
            cwd=str(app_dir)
        )
        
        # 等待服务器启动
        print("⏳ 等待服务器启动（约5-10秒）...")
        time.sleep(8)
        
        # 自动打开浏览器
        url = "http://localhost:8501"
        print(f"\n✅ 系统启动成功！")
        print(f"🌐 访问地址: {url}\n")
        
        try:
            webbrowser.open(url)
            print("✅ 已自动打开浏览器")
        except:
            print(f"⚠️  请手动打开浏览器访问: {url}")
        
        print("\n" + "=" * 60)
        print("💡 提示：关闭此窗口将停止系统")
        print("=" * 60 + "\n")
        
        # 等待用户关闭
        try:
            process.wait()
        except KeyboardInterrupt:
            print("\n\n正在关闭系统...")
            process.terminate()
            process.wait()
            print("✅ 系统已关闭")
    
    except Exception as e:
        print(f"❌ 启动失败: {e}")
        import traceback
        traceback.print_exc()
        input("\n按回车键退出...")

if __name__ == "__main__":
    main()
"@ | Set-Content "launcher.py" -Encoding UTF8
```

### 第四步：执行打包

```powershell
# 确保在项目根目录
cd D:\mlflow_learning_project

# 激活环境
conda activate uav_advupdfa

# 方式1：使用spec文件打包（推荐）
pyinstaller build_app.spec

# 方式2：直接打包launcher.py（更简单）
pyinstaller --name="无人机素材生成系统" --onefile --console --add-data "agents;agents" --add-data "app;app" --add-data "configs;configs" launcher.py

# 打包完成后，exe文件在 dist 目录
```

### 第五步：整理发布包

```powershell
# 创建发布目录
$releaseDir = "D:\无人机素材生成系统_发布版"
New-Item -ItemType Directory -Force -Path $releaseDir

# 复制exe文件
Copy-Item "dist\无人机素材生成系统.exe" -Destination $releaseDir

# 复制必要的模型文件（如果有）
New-Item -ItemType Directory -Force -Path "$releaseDir\models"
# Copy-Item "models\*.pt" -Destination "$releaseDir\models" -ErrorAction SilentlyContinue

# 创建使用说明
@"
# 无人机素材多角度生成与分析系统

## 系统要求
- Windows 10/11
- 至少 4GB 内存
- 至少 5GB 可用磁盘空间
- 推荐使用Chrome或Edge浏览器

## 使用方法

1. 双击运行 `无人机素材生成系统.exe`
2. 等待5-10秒，系统会自动打开浏览器
3. 如果浏览器未自动打开，请手动访问: http://localhost:8501
4. 在Web界面中上传图片并开始使用

## 功能说明

### 主要功能
- 📸 多角度素材生成（最多100张）
- 📊 8维度质量分析（雷达图）
- 🎯 自动增强训练（提升素材质量）
- 📦 检测框可视化（YOLO目标检测）
- 📈 置信度统计图表

### 使用步骤
1. 点击"上传一张无人机图片"
2. 点击"生成多角度素材并分析"
3. 查看生成的素材和8维度分析结果
4. 如果质量较差，点击"开始增强训练"
5. 查看增强后的结果和提升幅度

## 注意事项

- 首次启动可能需要30-60秒（加载模型）
- 生成素材时请耐心等待
- 建议使用1920x1080或更高分辨率的图片
- 关闭程序时请关闭此窗口

## 技术支持

如有问题，请联系开发团队。
"@ | Set-Content "$releaseDir\使用说明.txt" -Encoding UTF8

Write-Host "✅ 发布包已创建在: $releaseDir" -ForegroundColor Green
```

---

## 方案二：便携式启动包

如果PyInstaller打包文件太大，可以使用便携式方案：

### 第一步：创建启动脚本

```powershell
@"
@echo off
chcp 65001 >nul
title 无人机素材生成系统

echo ========================================
echo 🚁 无人机素材多角度生成与分析系统
echo ========================================
echo.

REM 检查Python环境
python --version >nul 2>&1
if errorlevel 1 (
    echo ❌ 错误：未检测到Python环境
    echo 请先安装Python 3.9或更高版本
    pause
    exit /b 1
)

REM 检查conda环境
call conda activate uav_advupdfa
if errorlevel 1 (
    echo ⚠️  警告：conda环境激活失败，使用系统Python
)

echo ✅ 环境检查通过
echo.
echo 🌐 正在启动Web服务器...
echo 请稍候5-10秒...
echo.

REM 启动Streamlit
start /B streamlit run app\web\material_generator_app.py --server.port 8501

REM 等待服务器启动
timeout /t 8 /nobreak >nul

REM 打开浏览器
start http://localhost:8501

echo.
echo ========================================
echo ✅ 系统已启动！
echo 🌐 访问地址: http://localhost:8501
echo ========================================
echo.
echo 💡 提示：关闭此窗口将停止系统
echo.

pause
"@ | Set-Content "启动系统.bat" -Encoding UTF8
```

### 第二步：创建依赖检查脚本

```powershell
@"
@echo off
chcp 65001 >nul
title 依赖检查工具

echo ========================================
echo 🔍 检查系统依赖...
echo ========================================
echo.

python --version
if errorlevel 1 (
    echo ❌ Python未安装
) else (
    echo ✅ Python已安装
)

echo.
echo 检查Python包...
python -c "import streamlit; print('✅ streamlit')" 2>nul || echo ❌ streamlit未安装
python -c "import torch; print('✅ torch')" 2>nul || echo ❌ torch未安装
python -c "import cv2; print('✅ opencv-python')" 2>nul || echo ❌ opencv-python未安装
python -c "import plotly; print('✅ plotly')" 2>nul || echo ❌ plotly未安装
python -c "import ultralytics; print('✅ ultralytics')" 2>nul || echo ❌ ultralytics未安装

echo.
echo ========================================
echo 如果缺少依赖，请运行：
echo pip install -r requirements.txt
echo ========================================
pause
"@ | Set-Content "检查依赖.bat" -Encoding UTF8
```

---

## 方案三：一键启动脚本（最简单）

创建 `演示启动.ps1`：

```powershell
@"
# 无人机素材生成系统 - 一键启动脚本

Write-Host "========================================" -ForegroundColor Cyan
Write-Host "🚁 无人机素材多角度生成与分析系统" -ForegroundColor Yellow
Write-Host "========================================" -ForegroundColor Cyan
Write-Host ""

# 检查conda环境
if (Get-Command conda -ErrorAction SilentlyContinue) {
    Write-Host "✅ 检测到Conda环境" -ForegroundColor Green
    conda activate uav_advupdfa
} else {
    Write-Host "⚠️  未检测到Conda，使用系统Python" -ForegroundColor Yellow
}

# 检查Python
try {
    $pythonVersion = python --version 2>&1
    Write-Host "✅ $pythonVersion" -ForegroundColor Green
} catch {
    Write-Host "❌ Python未安装或未添加到PATH" -ForegroundColor Red
    Read-Host "按回车键退出"
    exit
}

Write-Host ""
Write-Host "🌐 正在启动Web服务器..." -ForegroundColor Cyan
Write-Host "请稍候5-10秒..." -ForegroundColor Yellow
Write-Host ""

# 启动Streamlit（后台运行）
Start-Process python -ArgumentList "-m", "streamlit", "run", "app\web\material_generator_app.py", "--server.port", "8501" -WindowStyle Hidden

# 等待服务器启动
Start-Sleep -Seconds 8

# 打开浏览器
Start-Process "http://localhost:8501"

Write-Host ""
Write-Host "========================================" -ForegroundColor Cyan
Write-Host "✅ 系统已启动！" -ForegroundColor Green
Write-Host "🌐 访问地址: http://localhost:8501" -ForegroundColor Cyan
Write-Host "========================================" -ForegroundColor Cyan
Write-Host ""
Write-Host "💡 提示：关闭此窗口将停止系统" -ForegroundColor Yellow
Write-Host ""

Read-Host "按回车键关闭"
"@ | Set-Content "演示启动.ps1" -Encoding UTF8
```

---

## 测试与演示准备

### 第一步：准备测试图片

```powershell
# 确保有测试图片
$testImageDir = "test_images"
if (-not (Test-Path $testImageDir)) {
    New-Item -ItemType Directory -Force -Path $testImageDir
    Write-Host "⚠️  请将测试图片放入 $testImageDir 目录" -ForegroundColor Yellow
}
```

### 第二步：创建演示检查清单

```powershell
@"
# 演示前检查清单

## 环境检查
- [ ] Python环境正常
- [ ] 所有依赖包已安装
- [ ] YOLO模型可以正常加载
- [ ] GPU可用（如果使用GPU加速）

## 功能测试
- [ ] 图片上传功能正常
- [ ] 多角度生成功能正常
- [ ] 8维度分析显示正常
- [ ] 增强训练功能正常
- [ ] 保存功能正常

## 演示准备
- [ ] 准备3-5张测试图片（不同场景）
- [ ] 准备演示脚本/说明
- [ ] 测试网络连接（如果需要下载模型）
- [ ] 准备备用方案（如果网络问题）

## 性能优化
- [ ] 首次启动时间 < 60秒
- [ ] 生成18张图片时间 < 2分钟
- [ ] 增强训练时间 < 5分钟（单张）

## 演示建议
1. 先展示一张简单图片的生成结果
2. 展示8维度雷达图和分析
3. 展示增强训练功能
4. 展示保存和导出功能
"@ | Set-Content "演示检查清单.md" -Encoding UTF8
```

### 第三步：创建快速测试脚本

```powershell
@"
# 快速功能测试脚本

Write-Host "开始功能测试..." -ForegroundColor Cyan

# 测试1：导入模块
Write-Host "`n测试1: 导入核心模块..." -ForegroundColor Yellow
try {
    python -c "from agents.image_quality_analyzer import ImageQualityAnalyzer; print('✅ ImageQualityAnalyzer')"
    python -c "from agents.material_generator_agent import MaterialGeneratorAgent; print('✅ MaterialGeneratorAgent')"
    python -c "from agents.material_enhancement_trainer import MaterialEnhancementTrainer; print('✅ MaterialEnhancementTrainer')"
    python -c "from agents.image_multi_angle_generator import ImageMultiAngleGenerator; print('✅ ImageMultiAngleGenerator')"
    Write-Host "✅ 所有模块导入成功" -ForegroundColor Green
} catch {
    Write-Host "❌ 模块导入失败" -ForegroundColor Red
}

# 测试2：检查Streamlit
Write-Host "`n测试2: 检查Streamlit..." -ForegroundColor Yellow
try {
    python -c "import streamlit; print(f'✅ Streamlit {streamlit.__version__}')"
    Write-Host "✅ Streamlit正常" -ForegroundColor Green
} catch {
    Write-Host "❌ Streamlit未安装" -ForegroundColor Red
}

# 测试3：检查YOLO模型
Write-Host "`n测试3: 检查YOLO模型..." -ForegroundColor Yellow
try {
    python -c "from ultralytics import YOLO; model = YOLO('yolov8n.pt'); print('✅ YOLO模型加载成功')"
    Write-Host "✅ YOLO模型正常" -ForegroundColor Green
} catch {
    Write-Host "⚠️  YOLO模型可能需要下载" -ForegroundColor Yellow
}

Write-Host "`n✅ 功能测试完成！" -ForegroundColor Green
"@ | Set-Content "快速测试.ps1" -Encoding UTF8
```

---

## 推荐演示流程

### 给老板演示的步骤：

1. **启动系统**（30秒）
   - 双击启动脚本
   - 等待浏览器自动打开

2. **展示主界面**（1分钟）
   - 介绍8个分析维度
   - 说明系统功能

3. **演示生成功能**（3分钟）
   - 上传一张测试图片
   - 生成多角度素材（选择18-20张）
   - 展示生成的素材和检测框

4. **展示分析结果**（2分钟）
   - 展示8维度雷达图
   - 解释各个维度的含义
   - 展示置信度统计

5. **演示增强训练**（3分钟）
   - 点击"开始增强训练"
   - 展示提升幅度图表
   - 说明质量等级分类

6. **总结**（1分钟）
   - 强调系统优势
   - 说明应用场景

**总时长：约10-15分钟**

---

## 常见问题解决

### 问题1：打包后exe文件太大
**解决方案**：使用 `--exclude-module` 排除不需要的模块
```powershell
pyinstaller --exclude-module matplotlib --exclude-module jupyter launcher.py
```

### 问题2：启动后找不到模块
**解决方案**：在spec文件中添加hiddenimports
```python
hiddenimports=['agents.image_quality_analyzer', 'agents.material_generator_agent']
```

### 问题3：首次启动很慢
**解决方案**：这是正常的，因为需要加载模型。可以在启动时显示加载提示。

### 问题4：浏览器未自动打开
**解决方案**：手动访问 http://localhost:8501

---

## 最终发布包结构

```
无人机素材生成系统_发布版/
├── 无人机素材生成系统.exe    # 主程序（方案一）
├── 启动系统.bat              # 启动脚本（方案二）
├── 演示启动.ps1              # PowerShell启动脚本（方案三）
├── 使用说明.txt              # 用户手册
├── 演示检查清单.md            # 演示准备清单
└── README.txt                # 快速开始指南
```

---

## 下一步

1. **选择方案**：推荐方案一（PyInstaller）
2. **执行打包**：按照步骤执行
3. **测试验证**：在干净的环境测试
4. **准备演示**：准备测试图片和演示脚本
5. **打包分发**：创建最终发布包

**祝演示成功！** 🎉


