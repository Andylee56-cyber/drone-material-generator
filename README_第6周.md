# 第6周工作 - 无人机素材8维度智能分析Agent系统

## 🎯 项目目标

构建一个**智能化的无人机素材分析Agent系统**，能够：
- ✅ 输入图片，自动分析8个关键质量维度
- ✅ 基于分析结果评估素材质量
- ✅ 自动筛选高质量素材，代替人工找素材
- ✅ 生成详细的改进建议报告

## 📊 8个分析维度

1. **图片数据量** - 基于分辨率和文件大小
2. **拍摄光照质量** - 基于亮度、对比度、曝光分析
3. **目标尺寸** - 基于检测到的目标平均尺寸
4. **目标完整性** - 基于目标是否被裁剪或遮挡
5. **数据均衡度** - 基于不同类别目标的分布均衡性
6. **产品丰富度** - 基于检测到的目标类别数量
7. **目标密集度** - 基于单位面积内的目标数量
8. **场景复杂度** - 基于背景复杂度、纹理丰富度

## 🚀 快速开始

### 方式1: Web界面 (推荐)

```powershell
# 启动系统
.\start_material_analyzer.ps1

# 或手动启动
conda activate uav_adv
streamlit run app\web\material_analyzer_app.py --server.port 8502
```

访问: `http://localhost:8502`

### 方式2: 命令行

```powershell
# 批量分析
python scripts\batch_analyze.py `
  --input-dir "D:\无人机素材\原始图片" `
  --output-dir "D:\无人机素材\分析结果" `
  --min-quality 70
```

## 📁 项目结构

```
mlflow_learning_project/
├── agents/
│   ├── __init__.py
│   ├── image_quality_analyzer.py      # 8维度分析核心
│   └── material_generator_agent.py     # 素材生成Agent
├── app/web/
│   └── material_analyzer_app.py        # Streamlit Web界面
├── scripts/
│   ├── batch_analyze.py                # 批量分析脚本
│   └── generate_report.py              # 报告生成脚本
├── reports/                            # 分析报告输出目录
├── temp_uploads/                       # 临时上传文件目录
├── start_material_analyzer.ps1         # 快速启动脚本
└── 第6周工作部署文档.md                 # 详细部署文档
```

## 🔧 环境要求

- Python 3.10+
- Conda环境: `uav_adv`
- 依赖包: `ultralytics`, `plotly`, `streamlit`, `opencv-python`, `numpy`, `pandas`

## 📖 详细文档

查看 `第6周工作部署文档.md` 获取完整的使用说明和配置指南。

## 💡 使用示例

### 单图分析
```python
from agents.image_quality_analyzer import ImageQualityAnalyzer

analyzer = ImageQualityAnalyzer()
result = analyzer.analyze_single_image("图片.jpg")

for dim, score in result.items():
    print(f"{dim}: {score:.2f}%")
```

### 批量分析
```python
from agents.material_generator_agent import MaterialGeneratorAgent

agent = MaterialGeneratorAgent()
image_paths = ["img1.jpg", "img2.jpg", "img3.jpg"]
result = agent.analyze_and_evaluate(image_paths)

# 筛选高质量素材
high_quality = agent.filter_high_quality_materials(image_paths, min_score=80.0)
```

## 📈 输出结果

系统会生成：
- **8维度雷达图**: 可视化展示各维度得分
- **质量评估表格**: 每张图片的详细评分
- **改进建议**: 针对需要改进的维度提供具体建议
- **JSON报告**: 完整的分析结果数据
- **高质量素材列表**: 自动筛选的优质素材

## 🎉 预期成果

完成第6周工作后，将获得：
1. ✅ 完整的8维度分析系统
2. ✅ 智能素材筛选功能
3. ✅ 可视化分析界面
4. ✅ 详细的分析报告
5. ✅ 可扩展的架构

---

**创建日期**: 2024年11月  
**版本**: v1.0

