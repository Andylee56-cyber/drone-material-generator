# 🚀 5分钟部署到Streamlit Cloud - 完整指南

## ✅ 前置条件检查

在开始之前，确保：

- [ ] 代码已推送到GitHub
- [ ] 有GitHub账号（你的账号：`Andylee56-cyber`）
- [ ] 已配置Personal Access Token（GitHub不再支持密码推送）
  - 参考：`GitHub认证配置说明.md`
- [ ] 主应用文件是 `material_generator_app_optimized.py`
- [ ] 你的仓库：`https://github.com/Andylee56-cyber/drone-material-generator.git`

---

## 📝 第一步：准备GitHub仓库（2分钟）

### 如果代码已经在GitHub（你的情况）

```powershell
# 1. 切换到项目目录
cd D:\mlflow_learning_project

# 2. 检查Git状态
# 你的仓库：https://github.com/Andylee56-cyber/drone-material-generator.git
git status

# 3. 如果有修改，提交并推送
git add .
git commit -m "准备部署到Streamlit Cloud"
git push

# 注意：如果推送失败，需要配置Personal Access Token
# GitHub已不再支持密码推送，必须使用Token
# 创建Token：https://github.com/settings/tokens
```

### 如果代码还没在GitHub

```powershell
# 1. 在GitHub创建新仓库（网页操作）
# 访问：https://github.com/new
# 仓库名：drone-ai-system（或你喜欢的名字）
# 选择：Public（Streamlit Cloud需要公开仓库）

# 2. 初始化本地仓库
cd D:\mlflow_learning_project
git init
git add .
git commit -m "Initial commit"

# 3. 连接远程仓库（你的仓库：drone-material-generator）
# 注意：GitHub已不再支持密码推送，需要使用Personal Access Token
# 创建Token：访问 https://github.com/settings/tokens → Generate new token (classic)
# 权限选择：repo（全部权限）
git remote add origin https://github.com/Andylee56-cyber/drone-material-generator.git
git branch -M main
git push -u origin main
# 推送时会提示输入用户名：Andylee56-cyber
# 密码处输入：你的Personal Access Token（不是GitHub密码）
```

---

## 🌐 第二步：部署到Streamlit Cloud（3分钟）

### 步骤1：登录Streamlit Cloud

1. 访问：https://share.streamlit.io
2. 点击 "Sign in with GitHub"
3. 授权Streamlit访问你的GitHub账号

### 步骤2：创建新应用

1. 点击 "New app" 按钮
2. 填写信息：
   - **Repository**: 选择 `Andylee56-cyber/drone-material-generator`
   - **Branch**: `main`（或你的主分支）
   - **Main file path**: `material_generator_app_optimized.py`
   - **App URL**（可选）: 自定义名称，如 `drone-ai-system`
     - 最终网址：`https://drone-ai-system.streamlit.app`

3. 点击 "Deploy"

### 步骤3：等待部署

- 首次部署需要 3-5 分钟
- Streamlit会自动：
  - 检测 `requirements.txt`
  - 安装所有依赖
  - 启动应用

### 步骤4：检查部署状态

- ✅ 绿色 = 部署成功
- ⚠️ 黄色 = 正在部署
- ❌ 红色 = 部署失败（查看日志）

---

## 🔧 第三步：优化配置（可选）

### 使用优化的requirements.txt

如果你的 `requirements.txt` 很大，可以使用优化版本：

```powershell
# 复制优化版requirements
Copy-Item requirements_streamlit_cloud.txt requirements.txt -Force

# 提交更改
git add requirements.txt
git commit -m "优化requirements.txt for Streamlit Cloud"
git push
```

Streamlit Cloud会自动重新部署。

---

## 🐛 常见问题解决

### 问题1：部署失败 - 依赖安装错误

**解决方案**：
1. 检查 `requirements.txt` 格式
2. 确保所有包名正确
3. 查看部署日志找出具体错误

### 问题2：应用启动慢

**原因**：PyTorch等大包首次安装需要时间

**解决方案**：
- 使用 `requirements_streamlit_cloud.txt`（CPU版本）
- 或等待首次部署完成（后续会更快）

### 问题3：GPU相关错误

**原因**：Streamlit Cloud使用CPU

**解决方案**：
- 你的代码已经有CPU降级处理，应该没问题
- 如果还有错误，检查代码中的GPU硬编码

### 问题4：文件路径错误

**解决方案**：
- 确保所有文件路径使用相对路径
- 不要使用 `C:\` 或 `D:\` 等绝对路径

---

## 📊 部署后检查清单

部署成功后，检查以下功能：

- [ ] 应用可以正常打开
- [ ] 页面加载正常
- [ ] 图片上传功能正常
- [ ] 模型推理功能正常（CPU模式）
- [ ] 所有按钮和交互正常
- [ ] 没有错误提示

---

## 🔗 分享给老板

部署成功后，你会得到一个网址：

```
https://你的应用名.streamlit.app
```

**特点**：
- ✅ 自动HTTPS（安全）
- ✅ 24/7在线（不会sleep）
- ✅ 全球CDN加速
- ✅ 专业域名

**分享方式**：
- 直接发送链接
- 或生成二维码（使用在线工具）

---

## 🔄 更新应用

每次更新代码后：

```powershell
# 1. 提交更改
git add .
git commit -m "更新功能"
git push

# 2. Streamlit Cloud会自动重新部署（约1-2分钟）
```

---

## 💡 性能优化建议

### 1. 使用缓存

你的代码已经使用了 `@st.cache_resource`，很好！

### 2. 减少首次加载时间

- 延迟加载大型模型
- 使用进度条显示加载状态

### 3. CPU模式优化

- 你的代码已经设置了 `torch.set_num_threads(4)`
- 可以进一步优化批处理大小

---

## 📞 需要帮助？

如果遇到问题：

1. **查看部署日志**
   - 在Streamlit Cloud控制台点击应用
   - 查看 "Logs" 标签

2. **检查代码**
   - 确保所有import都正确
   - 检查文件路径

3. **联系我**
   - 提供错误日志
   - 我会帮你解决

---

## 🎉 完成！

部署成功后，你的应用就可以：
- ✅ 随时访问（24/7）
- ✅ 自动HTTPS
- ✅ 完全免费
- ✅ 自动更新

**恭喜！你的无人机AI系统已经上线！** 🚁✨

